#!/bin/sh
# Gradle wrapper script
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$SCRIPT_DIR/gradle/wrapper/gradle-wrapper.jar" "$@" 2>/dev/null || true

# Fallback: use system gradle if wrapper jar not present
if command -v gradle &>/dev/null; then
    gradle "$@"
fi
